node-linker=hoisted
shamefully-hoist=true

# Slow-network resilience: large tarballs (e.g. expo-image ~47MB) can exceed
# pnpm's default 60s fetch-timeout on slow links. Give them room + retries.
fetch-timeout=300000
fetch-retries=5
